{{--
    GNS Billing uses resources/views/layouts/app.blade.php as the single
    application layout and sidebar navigation.

    This legacy Bootstrap navbar is intentionally kept empty so it cannot
    introduce a second, inconsistent navigation system or duplicate menu.
--}}
