# Standalone Payment Invoice Print

The payment "Cetak" action opens a clean, standalone invoice page and the browser print dialog. The printable invoice includes all FIFO payment allocations for the transaction, with a fallback to the primary tagihan for legacy payments without allocations.
